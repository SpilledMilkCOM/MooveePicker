@model MoviePicker.WebApp.ViewModels.SharePicksViewModel

@helper DisplayTweet(string tweetText)
{
	@tweetText.Replace("%0a", "<p></p>")
}

@{
	ViewBag.Title = "Share Picks";
}

<div class="row">
	<div class="col-sm-5 col-md-offset-1">
		<h2>@ViewBag.Title</h2>
	</div>
</div>

<div class="row">
	<div class="col-sm-5 col-md-offset-1">

		<strong>JUST tweeting</strong> this page will embed this unique image using the <a href="https://developer.twitter.com/en/docs/tweets/optimize-with-cards/overview/abouts-cards" target="_blank">Twitter Cards</a>.
		The problem with these unique images is <strong>SPACE</strong>.
		They shouldn't be kept around for long.
		Another process may come along and remove the image after a while.
		I'll test what makes sense, but what makes sense to <strong>ME</strong> may not make sense to <strong>YOU</strong>.

		<div style="height: 30px;"> </div>

		<a class="twitter-share-button" data-size="large" href="https://twitter.com/intent/tweet?text=@ViewBag.TwitterTweetText">Tweet</a>

		<div style="height: 30px;"> </div>

		<div>
			Please <strong>NOTE</strong>: Since there is no database backing this website, it relies on only the current data provided by Fantasy Movie League.
			There is NO “as of” dating which means that once this reference is over a week old, then it will use the CURRENT BUX data and the picks will be using the OLD box office data contained within the tweet as well as reference different movies and different cost values.
			Sorry about that, but until this is backed by a database then these page image references WILL be out of date after a week.
		</div>
	</div>

	<div class="col-xs-10 col-sm-7 col-md-5">

		<div style="font-weight: bold; font-size: medium">Preview Tweet:</div>

		<div>
			@*
				@ViewBag.TwitterTweetText
				@ViewBag.TwitterTweetText.Replace("%0a", "<br />")
				@DisplayTweet(ViewBag.TwitterTweetText).ToHtmlString()
			*@
			@Html.Raw(ViewBag.TwitterTweetText.Replace("%0a", "<br />"))
		</div>

		<p> </p>

		<div class="movie-twitter-summary">
			<img class="img-responsive" style="border-top-left-radius: 10px; border-top-right-radius: 10px;" src="~/Images/@Model.TwitterImageFileName" />

			<div style="padding: 8px;">
				<div style="font-weight: bold; font-size: medium">@Model.TwitterTitle</div>
				<div style="font-size: small;">@Model.TwitterDescription</div>
			</div>
		</div>
	</div>
</div>
